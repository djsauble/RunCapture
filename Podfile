# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'RunCapture' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RunCapture
  pod 'SwiftWebSocket', '~> 2.6.2'

  target 'RunCaptureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RunCaptureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
